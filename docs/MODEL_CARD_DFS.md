# Model card — DFS

Version label: `dfs-2026.4.0-demo`

Current state: P20 floor, P50 median, P80 ceiling, projected volume, matchup context, availability, and salary are authored fictional fixture values. They are not generated by a trained distribution model. Inactive players are zeroed; questionable status remains explicit.

No guarantee language, operator action, or live market provider is allowed. Clean Mode hides this module behind a deliberate disclosure.

Production activation requires slate-lock feature snapshots, licensed availability and salary inputs, separate volume/efficiency modeling, explicit late-news scenarios, immutable artifacts, MAE/RMSE, quantile loss, interval coverage, distribution scoring, and historical backtests.
