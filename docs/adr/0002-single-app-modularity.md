# ADR 0002 — Single-app modularity

Keep one deployable preview with strict config/domain/provider/simulation/component boundaries. Split ingestion or compute only when live cadence, CPU, bundle, security, or release independence requires it.
