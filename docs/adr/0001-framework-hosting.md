# ADR 0001 — Framework and hosting

Use the bundled Vinext/Next.js App Router and Sites/Cloudflare Worker output. The workspace began empty, the stack supports server rendering and typed routes, and it preserves the supported preview path. Production DNS remains approval-gated.
