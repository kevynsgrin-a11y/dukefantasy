# ADR 0004 — Cache and refresh

Provider cadence is configuration, not a global constant. Normalized last-known-good snapshots and versioned keys are authoritative. Staleness derives from `source_as_of`; display/retention rights govern fallback.
